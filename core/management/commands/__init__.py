"""Django management command modules for TradeFlow Colón.

Each module exposes a ``Command`` subclass discovered by
``python manage.py <name>``. Prefer local/CI for seed and demo loaders;
reserve cron-oriented jobs for staging or production carefully.
"""
